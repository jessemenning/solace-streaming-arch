# Python
__pycache__/
*.pyc
.venv/

# Docker volumes
solace-data/

# Generated artifacts — regenerated on stack startup (run_demo.sh STEP 4)
# init.sql.default is committed as a fallback; init.sql is generated at runtime.
config/topic-mv-registry.yaml
config/risingwave/init.sql
!config/risingwave/init.sql.default

# Custom RisingWave build context (binary is ~700 MB — never commit)
risingwave-solace/risingwave-stripped

# Environment — never commit live credentials
.env
.env.local
docs/streaming-platform-comparison.docx
docs/streaming-platform-comparison.docx:Zone.Identifier
docs/trymetab.mhtml
